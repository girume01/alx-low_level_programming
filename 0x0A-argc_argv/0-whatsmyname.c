#include <stdio.h>
/**
 * main - entry point of program.
 * @argc: number of command line argumment.
 * @argv: an array of command line argument.
 * Return: 0 on success.
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
