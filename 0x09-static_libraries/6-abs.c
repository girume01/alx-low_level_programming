#include "main.h"
/**
 * _abs - computes the absolute val of intiger
 *
 * @x: the integer to check
 *
 * Return: the absolute value of int
 *
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
